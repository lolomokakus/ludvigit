#!/usr/bin/env bash
if [[ -n "$*" ]] ; then
  if [[ -t 1 ]] ; then
    espeak -v en --ipa "$*" 2>/dev/null
  else
    espeak -v en --stdout "$*" 2>/dev/null
  fi
else
  if [[ -t 1 ]] ; then
    espeak -v en --ipa --stdin 2>/dev/null
  else
    espeak -v en --stdout --stdin 2>/dev/null
  fi
fi
