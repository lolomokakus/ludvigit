#!/usr/bin/env bash
if [[ -n "$*" ]] ; then
  if [[ -t 1 ]] ; then
    espeak -v sv --ipa "$*" 2>/dev/null
  else
    espeak -v sv --stdout "$*" 2>/dev/null
  fi
else
  if [[ -t 1 ]] ; then
    espeak -v sv --ipa --stdin 2>/dev/null
  else
    espeak -v sv --stdout --stdin 2>/dev/null
  fi
fi
