#!/usr/bin/env bash
if [[ "$@" = "ethan great moves keep it up proud of you" ]]; then
	echo TRIGGERED
	exit
elif [[ "$@" = "" ]]; then
	echo error: no arguments given
	exit 1
else
	echo error: invalid argument: $1
	exit 2
fi
